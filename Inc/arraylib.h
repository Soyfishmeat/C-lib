#ifndef __LIB_H
#define __LIB_H

#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>

extern uint8_t *Check_Buffer(uint8_t *buff, uint16_t buff_len, uint8_t *data, uint16_t data_len);



#endif // __LIB_H
